# Reports

`balance-report.md` is a committed generated artifact.

Regenerate it with `npm run balance-report` after changing combat formulas, reward formulas, build archetypes, simulator behavior, or guardrails. CI uses `npm run balance-check` to verify that the current formulas still satisfy the guardrails.
